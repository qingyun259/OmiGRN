# Core training stack for OmiGRN
numpy>=1.23
pandas>=1.5
scikit-learn>=1.1

# Deep learning — pick the CPU / CUDA build from https://pytorch.org/get-started/locally/
# The reported runs used PyTorch 2.5.1; >=1.13 is enough for the genomic branch.
torch>=2.0

# Transcriptomic GRN-MPNN branch (scenarios S2 / S3 with --grn-edges) requires DGL.
# Install the wheel that matches your torch + CUDA/CPU build:
#   https://www.dgl.ai/pages/start.html   (reported runs used DGL 2.4.0)
# dgl
